version: "2"
services:
  builder:
    image: openlmis/requisition-ui
    volumes:
      - '.:/app'
    command:
      - './build.sh'
    env_file: .env
  image:
    build: .
    image: openlmis/requisition-refui